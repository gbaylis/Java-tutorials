/*
the while loop example 2 / Java
gbaylis.   

an if statement, as in if an integer is equal to a number, then stop the while loop
*/

public class Main {
    public static void main(String[] args) {
        // start counting at 0
        int marbles = 0;
        // run the code , while the conditions are true and print "total # of marbles"
        while(true) {
            System.out.println("> Total # of marbles: " + marbles);
            // the code starts counting at 0 and stops when the integer called marbles is equal to 5
            if(marbles == 5) {
                // break will end this portion of the code
                break;
            }
            // increment by 1
        marbles++;
            // this sentence will repeat along with the rest of the while loop
            System.out.println("Counting the # of marbles: ");
            
        }
    }
} 
